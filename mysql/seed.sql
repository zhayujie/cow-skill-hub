-- Agent Skill Hub - MySQL seed data.
--
-- Intentionally empty: import your real data instead, e.g. a dump exported from
-- the online instance:
--   mysql -u <user> -p <database> < your-exported-skills.sql
--
-- Tag definitions are created by schema.sql, so a fresh database already works
-- without any seed rows.

SET NAMES utf8mb4;
